<Window x:Class="AetherShell.Client.Windows.ExitPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Закрытие шелла" Height="220" Width="400"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Window.Effect>
        <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.8" Color="Black"/>
    </Window.Effect>

    <Border CornerRadius="15" BorderThickness="1" Background="#241820">
        <Border.BorderBrush>
            <SolidColorBrush Color="#FF5ED7"/>
        </Border.BorderBrush>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Введите пароль для закрытия шелла"
                       Foreground="White" FontWeight="Bold" FontSize="16"
                       HorizontalAlignment="Center" Margin="0,0,0,16"/>

            <PasswordBox x:Name="PasswordBox"
                         Grid.Row="1"
                         FontSize="14" Padding="10,8"
                         Margin="0,0,0,20"
                         Background="#0a1628" Foreground="White"
                         BorderBrush="#FF5ED7" BorderThickness="1"
                         PasswordChar="●"
                         KeyDown="PasswordBox_KeyDown">
                <PasswordBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </PasswordBox.Resources>
            </PasswordBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="BtnOk" Content="OK" Click="BtnOk_Click"
                        Width="100" Height="36" Margin="8,0"
                        Background="#FF5ED7" Foreground="White" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button x:Name="BtnCancel" Content="Отмена" Click="BtnCancel_Click"
                        Width="100" Height="36" Margin="8,0"
                        Background="#333" Foreground="White" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
