<Window x:Class="AetherShell.Client.Windows.MessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сообщение" Height="250" Width="450"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

	<Border CornerRadius="12" BorderThickness="1" Background="#F00A0612"
	        BorderBrush="#1FFFFFFF" Padding="24">
		<Border.Effect>
			<DropShadowEffect BlurRadius="28" ShadowDepth="0" Opacity="0.55" Color="#000000"/>
		</Border.Effect>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<TextBlock x:Name="TxtTitle" Text="Заголовок"
                       Foreground="White" FontWeight="SemiBold" FontSize="16"
                       HorizontalAlignment="Center" Margin="0,0,0,14"/>

			<TextBlock x:Name="TxtMessage" Text="Текст сообщения..."
                       Foreground="#A299B8" FontSize="13" TextWrapping="Wrap"
                       TextAlignment="Center" VerticalAlignment="Center"
                       Grid.Row="1"/>

			<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
				<Button x:Name="BtnYes" Content="OK" Click="BtnYes_Click"
                        Width="110" Height="36" Margin="8,0" Cursor="Hand"
                        Foreground="White" BorderThickness="0" FontSize="13">
					<Button.Template>
						<ControlTemplate TargetType="Button">
							<Border x:Name="Bg" Background="#FC5ED8" CornerRadius="8" Padding="12,0">
								<Border.Effect>
									<DropShadowEffect Color="#FB5ED8" BlurRadius="10" ShadowDepth="0" Opacity="0.45"/>
								</Border.Effect>
								<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="IsMouseOver" Value="True">
									<Setter TargetName="Bg" Property="Opacity" Value="0.92"/>
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Button.Template>
				</Button>

				<Button x:Name="BtnNo" Content="Отмена" Click="BtnNo_Click"
                        Width="110" Height="36" Margin="8,0" Cursor="Hand"
                        Foreground="White" FontSize="13" Visibility="Collapsed">
					<Button.Template>
						<ControlTemplate TargetType="Button">
							<Border x:Name="Bg" Background="#14FFFFFF" BorderBrush="#1FFFFFFF"
							        BorderThickness="1" CornerRadius="8">
								<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="IsMouseOver" Value="True">
									<Setter TargetName="Bg" Property="Background" Value="#28FFFFFF"/>
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Button.Template>
				</Button>
			</StackPanel>
		</Grid>
	</Border>
</Window>
