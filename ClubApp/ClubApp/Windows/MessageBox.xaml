<Window x:Class="AetherShell.Client.Windows.MessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сообщение" Height="250" Width="450"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

	<Window.Effect>
		<DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.8" Color="Black"/>
	</Window.Effect>

	<Border CornerRadius="15" BorderThickness="1" Background="#241820">
		<Border.BorderBrush>
			<SolidColorBrush Color="#FF5ED7"/>
		</Border.BorderBrush>

		<Grid Margin="20">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<TextBlock x:Name="TxtTitle" Text="Заголовок"
                       Foreground="White" FontWeight="Bold" FontSize="18"
                       HorizontalAlignment="Center" Margin="0,0,0,15"/>

			<TextBlock x:Name="TxtMessage" Text="Текст сообщения..."
                       Foreground="#E0E0E0" FontSize="14" TextWrapping="Wrap"
                       TextAlignment="Center" VerticalAlignment="Center"
                       Grid.Row="1"/>

			<StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">

				<Button x:Name="BtnYes" Content="OK" Click="BtnYes_Click"
                        Width="100" Height="35" Margin="10,0"
                        Background="#FF5ED7" Foreground="White" BorderThickness="0">
					<Button.Resources>
						<Style TargetType="Border">
							<Setter Property="CornerRadius" Value="8"/>
						</Style>
					</Button.Resources>
				</Button>

				<Button x:Name="BtnNo" Content="Отмена" Click="BtnNo_Click"
                        Width="100" Height="35" Margin="10,0"
                        Background="#FF2E2E" Foreground="White" BorderThickness="0"
                        Visibility="Collapsed">
					<Button.Resources>
						<Style TargetType="Border">
							<Setter Property="CornerRadius" Value="8"/>
						</Style>
					</Button.Resources>
				</Button>
			</StackPanel>
		</Grid>
	</Border>
</Window>