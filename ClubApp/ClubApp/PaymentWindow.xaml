<Window x:Class="AetherShell.Client.PaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Оплата AetherShell" Height="700" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" 
        WindowStyle="None" 
        AllowsTransparency="False"
        Background="#1A141B">

    <Window.Resources>
        <Color x:Key="ColorPrimaryGold">#FF5ED7</Color>
        <SolidColorBrush x:Key="AccentGoldBrush" Color="{StaticResource ColorPrimaryGold}"/>

        <Style x:Key="CloseBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentGoldBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource AccentGoldBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#FF5ED7" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#050818" Offset="0"/>
                        <GradientStop Color="#3C2F3D" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>

                <Rectangle VerticalAlignment="Bottom" Height="1">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="Transparent" Offset="0"/>
                            <GradientStop Color="{StaticResource ColorPrimaryGold}" Offset="0.5"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="💳" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Оплата услуг" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" ShadowDepth="2" BlurRadius="2"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <Button Click="BtnClose_Click" 
                        Style="{StaticResource CloseBtnStyle}"
                        Width="40" Height="40" 
                        HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Margin="0,0,10,0"
                        ToolTip="Закрыть окно оплаты">
                    <TextBlock Text="✕" FontSize="18" FontWeight="Bold"/>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Background="#1E1E2F">
                <wv2:WebView2 Name="webView"/>

                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock Text="Загрузка страницы оплаты..." Foreground="#666" FontSize="14" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>